# Module 04 Graded – Notes

---

## Summary Table

| GC    | Topic                         | Dependencies                     | Notes                                                                 |
| ----- | ----------------------------- | -------------------------------- | --------------------------------------------------------------------- |
| GC 16 | TextFormField + local storage | e.g. `shared_preferences`        | Persist text; restore on reopen.                                      |
| GC 17 | Email & password auth         | Backend or Firebase Auth         | Login/signup flow.                                                    |
| GC 18 | Comments from API             | `http` or `dio`                  | GET https://jsonplaceholder.typicode.com/comments; show name + email. |
| GC 19 | MVVM counter                  | Flutter SDK                      | Model-View-ViewModel; counter like default Flutter app.               |
| GC 20 | GET IT DONE + Firebase + auth | Firebase (Auth + Firestore/RTDB) | Todo app with cloud storage and email/password auth.                  |

---

## Per-assignment detail

- **GC 16** – `TextFormField`; save/load with `shared_preferences` (or similar).
- **GC 17** – Email/password auth; can use Firebase Auth or a custom backend.
- **GC 18** – HTTP client to fetch comments; parse JSON; display name and email.
- **GC 19** – Flutter-only; separate Model, View, ViewModel; counter increment (e.g. FAB).
- **GC 20** – Todo list UI + Firebase (Auth + database for tasks).

---
